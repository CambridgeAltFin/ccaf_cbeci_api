get:
  summary: Bitcoin network power demand
  tags:
    - Index
  parameters:
    - in: path
      name: price
      schema:
        type: number
      required: false
    - in: query
      name: p
      schema:
        type: number
      required: false
  responses:
    200:
      description: Successful operation
      content:
        application/json:
          schema:
            $ref: "../../schemas/DataStats.yaml"