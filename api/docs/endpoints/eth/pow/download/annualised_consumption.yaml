get:
  summary: Historical ETH annualised consumption
  parameters:
    - in: path
      name: version
      required: true
      schema:
        type: string
        enum:
          - v1.2.0
  tags:
    - Ethereum 1.0/Download
  responses:
    200:
      description: Successful operation
      content:
        text/csv:
          schema:
            type: string

