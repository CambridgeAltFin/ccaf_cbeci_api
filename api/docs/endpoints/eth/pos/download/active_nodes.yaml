get:
  summary: Ethereum number of active nodes
  parameters:
    - in: path
      name: version
      required: true
      schema:
        type: string
        enum:
          - v1.2.0
  tags:
    - Ethereum/Download
  responses:
    200:
      description: Successful operation
      content:
        text/csv:
          schema:
            type: string
